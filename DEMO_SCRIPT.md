# 🎥 语音日历工具 — Demo 演示脚本

> 预计时长：5-8 分钟  
> 录制工具：OBS / 系统自带录屏  
> 讲解方式：边操作边口播

---

## 第一部分：项目介绍（30秒）

```
口播：
大家好，我带来的作品是「语音日历工具」。
这是一款基于 Java SpringBoot 开发的语音智能日程管理应用。
通过语音输入一句话，就能自动创建日程，无需手动录入。
```

### 画面
- 打开 README.md，展示项目标题和介绍

---

## 第二部分：技术架构（30秒）

```
口播：
项目采用经典的 MVC 分层架构：Controller → Service → Mapper → Entity。
后端使用 SpringBoot 2.7，数据库用 MySQL 8.0，持久层用 MyBatis。
集成了 SpringDoc OpenAPI 自动生成接口文档。
```

### 画面
- 打开项目文件树结构，快速扫过各层代码

---

## 第三部分：Swagger 接口文档（40秒）

```
口播：
启动项目后，访问 swagger-ui.html 可以看到所有接口。
这里列出了全部 9 个 REST API：
语音创建日程、查询全部、按日期范围查询、删除、更新提醒、
批量删除、批量更新提醒、关键词搜索，以及语音文件上传。
每个接口都可以直接在页面上测试。
```

### 操作步骤
1. 终端运行 `mvn spring-boot:run`
2. 浏览器打开 `http://localhost:8080/swagger-ui.html`
3. 逐个展示各接口分组
4. 展开 POST `/schedule/voiceAdd` 接口

---

## 第四部分：核心演示 — 语音创建日程（60秒）

```
口播：
这是最核心的功能。我们模拟语音识别后的文本，输入「明天下午3点项目周会」，
系统会自动解析出时间和事件，创建一条日程记录。
```

### 操作步骤
1. 在 Swagger 中点击 `POST /schedule/voiceAdd` → Try it out
2. voiceText 输入：`明天下午3点项目周会`
3. 点击 Execute，展示返回 `code:200, msg:操作成功`

```
口播：
再试几个例子——
「后天上午10点交实训报告」
「2025-12-30 14:00 年终总结会议」
都能准确解析。
```

4. 依次测试上述 3 条语音文本

---

## 第五部分：前端页面演示（50秒）

```
口播：
项目还提供了一个简洁的前端页面，打开浏览器就能使用。
```

### 操作步骤
1. 打开 `http://localhost:8080`
2. 展示输入框、创建按钮、日程列表
3. 输入「今晚8点健身」→ 点击创建
4. 展示新日程出现在卡片的列表中（含标题和时间）
5. 点击提醒切换按钮（🔔 ↔ 🔕）
6. 点击删除按钮，确认删除
7. 展示日期筛选功能：选择日期范围 → 点击筛选

---

## 第六部分：额外功能展示（40秒）

```
口播：
除了基础功能，还实现了批量操作和搜索。
```

### 操作步骤
1. 切回 Swagger 页面
2. 展示 `DELETE /schedule/batchDelete?ids=1,2,3` — 批量删除
3. 展示 `PUT /schedule/batchRemind?ids=4,5&isRemind=1` — 批量开启提醒
4. 展示 `GET /schedule/search?keyword=会议` — 关键词搜索

```
口播：
另外还支持直接上传语音文件，接口会自动校验文件类型和大小。
```

5. 展示 `POST /voice/upload` 接口，说明参数格式

---

## 第七部分：代码亮点（40秒）

```
口播：
再看几个代码层面的亮点。
```

### 画面
1. 打开 `ScheduleServiceImpl.java`，展示智能时间解析引擎代码
2. 打开 `R.java`，展示统一返回格式
3. 打开 `GlobalExceptionHandler.java`，展示全局异常处理
4. 打开 Git 提交记录，展示 9 条规范 commit + 6 个 PR

---

## 第八部分：总结（20秒）

```
口播：
以上就是语音日历工具的全部功能展示。
本项目完全自主开发，代码开源在 GitHub。
感谢观看！
```

### 画面
- GitHub 仓库首页，展示 README 和 PR 记录

---

## 📋 录制清单

| # | 场景 | 时长 | 画面 |
|---|------|------|------|
| 1 | 项目介绍 | 30s | README |
| 2 | 技术架构 | 30s | 文件树 |
| 3 | Swagger 文档 | 40s | Swagger UI |
| 4 | 语音创建（核心） | 60s | Swagger + 3次测试 |
| 5 | 前端页面 | 50s | 浏览器交互 |
| 6 | 额外功能 | 40s | Swagger API |
| 7 | 代码亮点 | 40s | IDE 代码 |
| 8 | 总结 | 20s | GitHub 首页 |

---

## 🔧 录前准备

```bash
# 1. 初始化数据库
mysql -u root -p < init.sql

# 2. 修改 application.yml 中的数据库密码

# 3. 启动项目
mvn spring-boot:run

# 4. 确认以下地址可访问：
# http://localhost:8080/schedule/list
# http://localhost:8080/swagger-ui.html
# http://localhost:8080
```
